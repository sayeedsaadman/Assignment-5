1️⃣ What is the difference between var, let, and const?
Answer: var can be updated , also redeclare and its functional scope
let can be updated , also redeclare and its Block scope
const cannot be updated or redeclare .


2️⃣ What is the spread operator (...)?
Answer: Spread Operator (...) means it expand elements of an array or object.

3️⃣ What is the difference between map(), filter(), and forEach()?
Answer: 
map() : Creates a new array after transform each element.
Filter() : Create a new array with elements that pass a condition.
foreach() : Use Loop through array.

4️⃣ What is an arrow function?
Answer: Its basically a function using syntax for writing a javascript usning => 

5️⃣ What are template literals?
Answer: Strings written with backticks `` that allow variables using ${} in JavaScript.